<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="filippovvitaliyleonidovich.bstu.fit.lab2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StaffInfo"></activity>
        <activity android:name=".managerpage.ManagerMainInfo" />
        <activity android:name=".studentpage.studentInfo" />
        <activity
            android:name=".registration.reg_anotherinformation"
            android:launchMode="singleTop" />
        <activity
            android:name=".registration.reg_role"
            android:launchMode="singleTop" />
        <activity
            android:name=".registration.reg_name"
            android:launchMode="singleTop" />
        <activity android:name=".Entry" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>